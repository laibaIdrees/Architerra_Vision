import 'package:flutter/material.dart';
import 'package:flutter_login/screens/BluePrints/components/body.dart';
class Blueprints_images_screen extends StatelessWidget {
  const Blueprints_images_screen({super.key});

  @override
 Widget build(BuildContext context) {
    // Size size = MediaQuery.of(context).size;
    return Scaffold(
      body: Body(),
    );
  }
}